remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

title: PyExperimenter
description: Automatic execution of experiments, capturing corresponding results in a unified manner in a database.
logo: py-experimenter.logo.png
